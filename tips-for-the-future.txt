JUCE LIBRARY KNOWLEDGE

unique_ptr is used for pointers that will always have the reference to the object
useful for the root object, such as the button of a component.
but not if reference not created for that pointer specifically

Use Init/cleanup for everything that needs any processing time